{
  "name": "AmaJSON exporter",
  "version": "0.9",
  "manifest_version": 2,
  "description": "When visiting 'Your Highlights' on Amazon, context-click on a book title and this will export it as a book as a JSON structure",
  "background": {
    "persistent": true,
    "scripts": ["background.js"]
  },
  "permissions": [
    "activeTab",
    "clipboardWrite",
    "contextMenus",
    "background"
  ]
}
